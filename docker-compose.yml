services:
  us-stock:
    build:
      context: .
    container_name: us-stock
    ports:
      - "3000:3000"
    environment:
      - HOSTNAME=0.0.0.0
      - PORT=3000
      - TZ=America/New_York
    restart: unless-stopped
