⏺ 분석 결과와 권고안을 정리합니다.

  분석 결과

  두 폴더는 이름만 같고 역할이 완전히 다릅니다.

  ┌──────────────────────┬────────────────────────────────────────────┬──────────────────────────────────────────┐
  │         폴더         │                 실제 용도                  │                   내용                   │
  ├──────────────────────┼────────────────────────────────────────────┼──────────────────────────────────────────┤
  │                      │ AI 에이전트 운영 artifact — research-lead  │ memory.md (성공/실패 학습), backlog.md   │
  │ docs/research/       │ 에이전트가 read/write하는 Reflexion 메모리 │ (P0/P1/P2 팩터 후보), hypotheses/ (빈    │
  │                      │                                            │ 폴더)                                    │
  ├──────────────────────┼────────────────────────────────────────────┼──────────────────────────────────────────┤
  │                      │                                            │ 2024-2026 전략 MD, 뉴스 스크래퍼 도구    │
  │ docs/notes/research/ │ 사람이 작성한 참고 리서치 노트             │ 목록, 투자 명언 모음, strategies/        │
  │                      │                                            │ 서브폴더                                 │
  └──────────────────────┴────────────────────────────────────────────┴──────────────────────────────────────────┘

  겹치는 점: "research"라는 이름만. 내용·작성 주체·소비자 모두 다름.

  코드 참조 확인: grep 결과 실제 실행 코드에서 이 경로를 읽는 곳은 없음. agent 프롬프트
  문서(docs/agents/PERFORMANCE-GUIDE.md, docs/agents/daily-performance-prompts.md)에서 .docs/research/ 경로
  placeholder로 언급하지만 실제 파일 경로 하드코딩은 아님.

  권고안

  합치기는 부적절 — 기계용 운영 artifact + 사람용 참고자료를 섞으면 혼란만 증가.

  이름 변경 권고:
  - docs/research/ → 그대로 유지 (에이전트 시스템이 참조하는 경로, 변경 시 agent 프롬프트까지 같이 수정 필요)
  - docs/notes/research/ → docs/notes/reference/ 로 이름 변경 (참고 자료의 성격이 reference에 더 맞음)
  - 덤으로 docs/notes/research/README.md 내용이 실제 구조와 불일치(improvements/를 하위 폴더로 안내하지만 실제로는
  docs/improvements/가 상위에 따로 존재) → 정정

  이 방향으로 진행해도 될지, 아니면 다른 이름(library/, reading/, sources/ 등)을 선호하는지 알려주세요. 확정되면 git
  mv로 히스토리 보존해서 이동하고 README 정비합니다.

✻ Churned for 2m 1s · 1 shell still running